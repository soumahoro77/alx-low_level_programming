#_MAIN_H_ ifndef
#définir _MAIN_H_

non signé int binary_to_uint(const char *b);
void print_binary(non signé long int n);
int _putchar(char c);
int get_bit(long int n non signé, index int non signé);
int set_bit(non signé long int *n, index int non signé);
int clear_bit(non signé long int *n, index int non signé);
non signé int flip_bits(non signé long int n, non signé long int m);
int get_endianness(nul);

#endif
